# AIDI — 삼성 갤럭시 탭(Android)에서 실행하기

이 압축 파일은 **PC용 설치 없이** 태블릿에서 **Termux**로 서버를 띄운 뒤, **같은 기기 브라우저**로 쓰는 방법입니다.

> ⚠️ Google Play에 있는 가짜 Termux가 있습니다. **F-Droid**에서 **Termux** (com.termux) 를 설치하세요.

## 1. 준비

1. **F-Droid** 설치 → **Termux** 설치  
2. 압축 `aidi-samsung-tablet.zip` 을 태블릿으로 복사  
   - USB, Google Drive, 이메일 등  
3. Termux에서 저장소 접근(선택):
   ```bash
   termux-setup-storage
   ```
4. 압축 풀기 (예: Downloads에 넣었다면):
   ```bash
   pkg update && pkg install -y unzip
   cd ~/storage/downloads
   unzip aidi-samsung-tablet.zip -d ~
   cd ~/aidi-samsung-tablet   # 폴더명은 압축 구조에 맞게 조정
   ```

## 2. 실행

```bash
chmod +x run-tablet.sh
./run-tablet.sh
```

처음 실행 시 Python 패키지 설치에 **3~10분** 걸릴 수 있습니다.

## 3. 화면 열기

태블릿 **Chrome** 또는 **삼성 인터넷**에서:

**http://127.0.0.1:8000**

(서버를 끄려면 Termux에서 `Ctrl+C`)

## 4. 자주 나는 문제

| 증상 | 해결 |
|------|------|
| `python3: command not found` | `pkg install -y python` |
| `numpy` 설치 실패 | `pkg install -y python-numpy` 후 `./run-tablet.sh` 다시 |
| 인터넷/API 오류 | Wi‑Fi 연결, 방화벽·VPN 확인 |
| 화면이 좁음 | 가로 모드 권장 · 900px 이하는 세로 레이아웃 |

## 5. PC에서 쓰기

Windows/Mac/Linux:

```bash
chmod +x run.sh
./run.sh
```

브라우저: http://localhost:8000

## 6. 보안

- API 키는 **본인 기기에만** 저장됩니다 (`backend/data/`).
- 압축 파일을 타인과 공유하지 마세요.
- 실거래 API 키는 테스트 후에만 사용하세요.

## 구조

- `backend/` — Python FastAPI 서버  
- `frontend/dist/` — 이미 빌드된 화면 (태블릿에서 npm 불필요)  
- `run-tablet.sh` — Termux용 실행 스크립트  
- `run.sh` — PC용 (빌드 포함)
